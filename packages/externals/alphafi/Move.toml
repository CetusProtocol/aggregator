[package]
name = "alphafi_liquid_staking"
edition = "2024.beta" # edition = "legacy" to use legacy (pre-2024) Move
published-at = "0x059f94b85c07eb74d2847f8255d8cc0a67c9a8dcc039eabf9f8b9e23a0de2700"
# license = ""           # e.g., "MIT", "GPL", "Apache 2.0"
# authors = ["..."]      # e.g., ["Joe Smith (joesmith@noemail.com)", "John Snow (johnsnow@noemail.com)"]

[dependencies]
Sui = { git = "https://github.com/MystenLabs/sui.git", subdir = "crates/sui-framework/packages/sui-framework", rev = "mainnet" }
SuiSystem = { git = "https://github.com/MystenLabs/sui.git", subdir = "crates/sui-framework/packages/sui-system", rev = "mainnet" }


# For remote import, use the `{ git = "...", subdir = "...", rev = "..." }`.
# Revision can be a branch, a tag, and a commit hash.
# MyRemotePackage = { git = "https://some.remote/host.git", subdir = "remote/path", rev = "main" }

# For local dependencies use `local = path`. Path is relative to the package root
# Local = { local = "../path/to" }

# To resolve a version conflict and force a specific version for dependency
# override use `override = true`
# Override = { local = "../conflicting/version", override = true }

[addresses]
alphafi_liquid_staking = "0xc35ee7fee75782806890cf8ed8536b52b4ba0ace0fb46b944f1155cc5945baa3"

#UpgradeCap: 0x31a1385dd1c8a1f44369f081f29072155880816ffd170ceff67eecd506c557c0


# Named addresses will be accessible in Move as `@name`. They're also exported:
# for example, `std = "0x1"` is exported by the Standard Library.
# alice = "0xA11CE"

[dev-dependencies]
# The dev-dependencies section allows overriding dependencies for `--test` and
# `--dev` modes. You can introduce test-only dependencies here.
# Local = { local = "../path/to/dev-build" }
 
[dev-addresses]
# The dev-addresses section allows overwriting named addresses for the `--test`
# and `--dev` modes.
# alice = "0xB0B"


#0x55cdf0b2149c2256c8e0bc0031e867aaa4b64c3036192874da229268dc5240bc LiquidStakinInfo

#0xa053038da33d1ebf6050c8df379f9926e097182b94a416bef121f0136b8734f7 UpgradeCap

#0x63212268e3a64a61c1d1d42abee786c29703b52e7945cb5465db897626c8ac04 st_sui trasury cap

#0xb4c01875b7a3cff6a90b07e2fbef28558473b3d91c39a5fbc7086e9a126830df  Admin Cap 

